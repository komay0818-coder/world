# 星辰 DEV－重新對話接續指南

最後更新：2026-08-13  
目前顯示版本：`0.6.8`
GitHub：`https://github.com/komay0818-coder/world`  
開發分支：`dev`

## 新對話開場指令

請在新對話直接貼上：

> 前往 `C:\Users\User\OneDrive\文件\放置\.github-pages`，切換至 `dev` 分支，完整讀取 `CONTINUE_DEVELOPMENT.md` 與 `DEVELOPMENT_PROGRESS.md`，然後接續開發「星辰 DEV」。開始修改前先確認工作目錄沒有未提交變更。

## 目前 Git 狀態基準

- 目前本機最新提交：`45c16a2`（固定載入第二章裝備來源版本）；本次尚未推送。
- 第二章配方與紫裝來源功能提交：`2728da1`。
- 第二章一般材料功能提交：`32d9183`。
- 上一個已推送的功能入口提交：`9b023d1`（固定載入技能書合成版本）。
- 魔法塔技能書合成功能提交：`0ecf19b`。
- 新對話應以遠端 `dev` 最新提交為準，不要硬退回上述雜湊。
- 第二章淨化功能提交：`88042116bfbcc936d5b561889e63745c21c27881`。
- 多分頁角色提交：`f6160674d8daa5e7d6b03b8c66608bbbeca43133`。
- 目前完整測試數量：58。

## 專案與發布規則

- 實際專案路徑：`C:\Users\User\OneDrive\文件\放置\.github-pages`。
- 一律在 `dev` 分支開發並推送 `origin/dev`。
- `index.html` 會以 raw.githack 固定載入特定提交的 `script.js` 與部分政策檔；改動這些檔案後必須先提交功能，再將入口固定到該功能提交，最後再提交入口固定。
- 每次完成修改都要更新 `DEVELOPMENT_PROGRESS.md`、提升顯示版本、補測試、執行全部測試與 `git diff --check`。
- 不得覆蓋或刪除使用者已有變更；開始前先查看 `git status --short`。

## 最新完成：第二章配方與紫裝來源

- 六張製作配方已指定第二章各地圖怪物，沿用 1% 稀有配方機制且沒有保底。
- 配方可實際掉落與存檔；製作材料與金幣未設定，因此尚未開放工作坊製作。
- 黑石督軍戰盔、腐化守護者皮靴、幽森魔珠、墮落荊棘魔杖已建立直接掉落來源資料。
- 四件紫裝的機率、數值、詞綴、特殊能力與圖片仍待玩家後續規畫；在資料完整前不進入實際擲骰。
- 掉落查詢會依當前地圖顯示配方與專屬紫裝，紫裝明確標示機率與能力待定。
- Debuff、一般材料與建築 Lv.3 規則均未修改；58 個 JavaScript 測試通過。

## 先前完成：第二章一般材料與建築 Lv.3

- 第二章一般材料為黑木、厚皮、蜘蛛絲、毒囊、黑鐵礦石、腐化結晶；厚皮沿用既有 `hard-hide` ID。
- 材料依指定怪物各自擲骰，沒有加入第二章整張地圖的通用掉落。
- 五棟建築已開放 Lv.2 → Lv.3，每棟固定消耗 30,000 金幣與指定第二章材料。
- 掉落查詢已同步，只顯示當前地圖實際可取得的材料。
- Debuff 解除道具、掉落率、消耗、成功率與無保底規則未修改；第二章裝備製作需求仍待後續規畫。
- 56 個 JavaScript 測試與語法、格式檢查全數通過。

## 先前完成：魔法塔與技能殘頁合成

- 村莊「符文塔」已更名為「魔法塔」，沿用既有 `rune` 建築 ID 與升級資料。
- 每次合成消耗同階技能殘頁 10 個，成功率 40%；成功獲得同階技能書 1 本，失敗仍消耗殘頁。
- 魔法塔 Lv1 可合成初級技能書；Lv2 保留初級合成並解鎖中級技能書。
- 合成功能直接使用 `skill-upgrade-policy.js` 內既有的技能殘頁與技能書，不建立新的魔法碎片或魔法結晶物品。
- 55 個 JavaScript 測試、主程式與政策語法及 `git diff --check` 均通過。
- `dev` 已推送並與 `origin/dev` 同步至 `9b023d1`；`main` 未修改或合併。

## 先前完成：第二章黑森林 Debuff 解除道具

黑森林共有 6 層全章共用 Debuff，首次進入第二章後啟用。永久進度保存在角色進度的 `blackForestCorruption.removedLayers`；舊版 `purifiedMapIds` 會自動轉換為已解除層數。

每次解除固定規則：

- 對應地圖解除道具 ×20。
- 金幣 ×1,000。
- 成功率 10%。
- 成功永久解除 1 層；失敗層數不變。
- 成功或失敗都消耗全部費用。
- 無保底；最低 0 層。

六種專屬道具：

| 地圖 | 解除道具 |
|---|---|
| 黑森林入口 | 森林淨化葉 |
| 黑森林小徑 | 黑石破咒石 |
| 蜘蛛巢穴 | 蛛毒淨化囊 |
| 黑石據點 | 督軍徽記碎片 |
| 森林祭壇 | 祭壇淨化結晶 |
| 黑森林深處 | 黑森林之心碎片 |

掉落規則：普通怪 10% ×1、菁英 25% ×1、Boss 100% ×1。不同地圖道具不可互相替代。掉落查詢依目前選擇地圖顯示，並提供淨化按鈕、目前層數、持有量與金幣狀態。

本次明確未加入第二章其他製作材料、建築升級材料與裝備製作需求。

## 第二章六張地圖怪物分布

1. 黑森林入口：普通怪「黑森林野狼、腐化野豬、暗影蜘蛛、枯木行者」；菁英「黑森林獵人」；Boss「森林守護者」。
2. 黑森林小徑：普通怪「黑石斥候、黑石掠奪者、黑石弓箭手、黑石毒蜘蛛」；菁英「黑石訓獸師、黑石隊長」；Boss「黑石百夫長」。
3. 蜘蛛巢穴：普通怪「黑石毒蜘蛛、噴毒蜘蛛、蛛網編織者、黑石毒獵手」；菁英「黑石訓獸師、黑石毒刃刺客」；Boss「巨大蜘蛛」。
4. 黑石據點：普通怪「黑石守衛、黑石弩手、黑石狂戰士、黑石戰犬」；菁英「黑石獅衛、黑石蠻角勇士」；Boss「黑石督軍」。
5. 森林祭壇：普通怪「腐化森林狼、荊棘魔藤、腐化黑石士兵」；菁英「祭壇守衛、腐化黑石祭司、墮落德魯伊」；Boss「腐化祭壇守護者」。
6. 黑森林深處：普通怪「腐化森林狼、腐化樹妖、黑暗孢子獸、森林之魂」；菁英「腐化黑石百夫長、腐化墮落德魯伊」；Boss「黑森林之心」。

## 多分頁角色狀態

- 每個分頁以 `sessionStorage['stardust-tab-active-character-slot']` 獨立綁定目前角色。
- 角色資料與進度仍放在共用 `localStorage['stardust-character-slots']`；舊版全域角色與進度鍵保留為相容鏡像。
- 兩個分頁可分別切換不同角色並練功，不會因另一分頁切換角色而跟著跳轉。
- 這是第一階段實作；尚未加入更進階的跨分頁同時寫入衝突鎖定。如未來發生同一角色同時在兩個分頁操作，需另行規劃鎖定或版本合併策略。

## 已確立的重要規則

- 掉落物查詢只顯示目前所在／選擇地圖可取得的裝備、材料、配方與技能材料；切換地圖即同步切換。
- 同名材料必須使用相同物品 ID，不可再出現「同名不同 ID」。
- 第一章黑礦石只由平原深處的黑石系列怪物掉落：黑石斥候 10%、黑石掠奪者 30%、黑石首領 100% ×2。
- 第一章護腕、斗篷、肩甲製作金幣已提高為原先 20 倍。
- 戰鬥畫面的寵物全身圖已移除，改為右下角小頭像；目前不顯示寵物等級、HP 或數值。
- 玩家戰場資訊條已隱藏；角色站位、玩家與隊友比例及怪物尺寸已依既有視覺規則調整。
- Boss 尺寸只比普通／菁英明顯一些，不得回復成過度巨大。
- 不要擅自新增前排／後排系統，也不要在純 UI 調整時修改戰鬥邏輯、公式、AI、技能或目標選擇。

## 第一章裝備圖片狀態

- 已新增並實裝大量第一章武器專屬圖片，包括單手劍、雙手劍、單手斧、雙手斧、弓、匕首、法杖及第二批森林系列武器。
- 已修正部分素材的白色棋盤背景，轉為真正 Alpha 透明 PNG。
- 獵人寵物頭像已有四種族素材：人類、夜精靈、不死族、半獸人。
- 詳細每次圖片與版本紀錄請完整讀取 `DEVELOPMENT_PROGRESS.md`。

## 核心檔案

- `script.js`：主要遊戲、戰鬥、UI、存檔與掉落結算整合。
- `black-forest-corruption-policy.js`：六層 Debuff、六種解除道具、掉落、消耗與成功率。
- `drop-lookup-policy.js`：依目前地圖建立掉落物索引。
- `chapter-two-map-policy.js`：第二章六張地圖架構。
- `black-forest-entrance-policy.js`、`black-forest-trail-policy.js`、`spider-nest-policy.js`、`blackstone-stronghold-policy.js`、`forest-altar-policy.js`、`black-forest-depths-policy.js`：各地圖怪物與戰鬥規則。
- `chapter-one-material-drop-policy.js`：第一章材料與黑礦石來源。
- `crafting-policy.js`：製作資料與金幣／材料需求。
- `DEVELOPMENT_PROGRESS.md`：完整歷史變更與規劃。
- `tests/`：目前 58 個測試檔。

## 驗證方式

使用專案指定 Node 執行：

```powershell
$node='C:\Users\User\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe'
& $node --check script.js
$failed=@()
Get-ChildItem tests -Filter '*.test.js' | ForEach-Object { & $node $_.FullName; if ($LASTEXITCODE -ne 0) { $failed += $_.Name } }
if ($failed.Count -gt 0) { throw ('Failed: ' + ($failed -join ', ')) }
git diff --check
```

## 下一次接續注意

1. 先完整讀取本文件與 `DEVELOPMENT_PROGRESS.md`。
2. 確認 `dev`、遠端最新狀態及工作目錄是否乾淨。
3. 不要重新實作上述已完成項目。
4. 以使用者新提出的需求為唯一下一步；目前沒有預設要新增的第二章製作材料。
