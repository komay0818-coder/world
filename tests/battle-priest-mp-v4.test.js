const assert=require('node:assert/strict'),{output,rates}=require('../tools/battle-priest-mp-v4-test.js');assert.deepEqual(Object.values(rates),[0,.01,.015]);for(const scenario of ['boss','five']){const r=output.results[scenario];assert.equal(r.baseline.mp.smite,0);assert.ok(r.p1.mp.smite>0);assert.ok(r.p1_5.mp.smiteGenerated>r.p1.mp.smiteGenerated);assert.equal(r.p1_5.mp.devotion,0);assert.ok(r.p1_5.combat.devotionHealing>0);}console.log('battle-priest-mp-v4: assertions passed');
